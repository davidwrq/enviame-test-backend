DROP DATABASE IF EXISTS test.db;
CREATE DATABASE test.db;
USE test.db;

CREATE TABLE `test.db`.`enterprise` 
);

USE test.db;

INSERT INTO `test.db`.`enterprise` (``) VALUES
	(),
	(),
	(),
	(),
	(),
	(),
	(),
	(),
	(),
	();

